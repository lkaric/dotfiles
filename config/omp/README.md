omp keeps its state and config under ~/.omp (config.yml, models.yml, sessions).
Once omp has been run once and the files exist, move config.yml / models.yml
here and link them from modules/home/dotfiles.nix if you want them tracked.
